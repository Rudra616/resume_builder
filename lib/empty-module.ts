// Stub used by the bundler alias for pdfjs-dist's optional `canvas` dependency,
// which is only needed for Node-side rendering that this app never performs.
const emptyModule = {};

export default emptyModule;
