node_modules
.next
out
build

# public/pdf.worker.min.mjs is committed on purpose: PDF import needs it served
# from our own origin, and hosts that run `next build` directly skip the
# copy-pdf-worker script. `postinstall` keeps it in step with pdfjs-dist.

.DS_Store
*.log
.env*
!.env.example
